import 'package:family_list/redux/state.dart';

// // todos: new List.from(state.todos)..add(action.todo),
AppState authReducer(AppState state, action) {
  print("in auth reducer");
  // if (action is UpdateNameAction) {
  //   return AppState(name: action.name, loggedIn: state.loggedIn);
  // } else if (action == Actions.ToggleLoggedInStatus) {
  //   print("action found");
  //   return AppState(name: state.name, loggedIn: !state.loggedIn);
  // } else {
  //   return state;
  // }
}
